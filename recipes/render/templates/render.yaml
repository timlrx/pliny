services:
  - type: web
    name: next-js
    env: node
    plan: starter
    buildCommand: yarn; yarn build
    startCommand: yarn start
    envVars:
      # ⚠️ Add other env variables that you might require
      # See .env.example for the list that is used
      - key: NODE_ENV
        value: production
